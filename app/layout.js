export const metadata = {
  title: 'Subcode Decode API',
  description: 'Personal Input Decoder — 7 subconscious domains powered by Claude AI',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
